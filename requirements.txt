# Versions en borne minimale (et non figées) pour laisser pip/uv choisir
# des wheels compatibles avec la version de Python de l'environnement de
# déploiement (Streamlit Cloud peut utiliser une version très récente de
# Python, pour laquelle d'anciennes versions figées n'ont pas de wheel).

# --- Frontend ---
streamlit>=1.38

# --- Backend / API ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5

# --- Base de données ---
SQLAlchemy>=2.0.35
psycopg[binary]>=3.2
alembic>=1.13

# --- Authentification ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# --- Graphiques / cartographie ---
plotly>=5.24
folium>=0.17
streamlit-folium>=0.23
# pyvista  # à réactiver en Phase 3 (Digital Twin) — nécessite des
# dépendances système (libGL, Xvfb) à déclarer dans packages.txt sur
# Streamlit Cloud avant de la réinstaller.

# --- IA ---
numpy>=1.26
scikit-learn>=1.5
# tensorflow  # à réactiver en Phase 4 si un modèle de deep learning
# s'avère nécessaire (poids/temps d'installation importants).

# --- Météo / temps réel ---
requests>=2.32
paho-mqtt>=2.1
pymodbus>=3.7
websockets>=13.1

# --- Exports ---
reportlab>=4.2
openpyxl>=3.1
pandas>=2.2

# --- Tests ---
pytest>=8.3
pytest-cov>=5.0
httpx>=0.27
